# Live ledger data — never commit
data/*.csv
data/*.log
data/*.json
data/*.txt
data/picks/
data/projections/
data/clv_exports/

# Operator-local threshold overrides (S4f-X) — the .example.toml is committed
config/thresholds.toml

# Exception: team_sigmas_*.json are STATIC calibration fixtures (team scoring
# distributions), not live ledger — committed so a fresh checkout / CI has them
# (audit P0.5 CI fix; the test suite loads them at import via calibrated.py).
!data/team_sigmas_nba.json
!data/team_sigmas_nhl.json
!data/team_sigmas_mlb.json
!data/team_sigmas_wnba.json

# Generated artefacts
results_graphics/
projections/*.csv
*.png
*.jpg
*.jpeg

# Research input notes (binary)
*.docx

# Generated Excel exports
data/*.xlsx

# Exported Claude Code session transcripts
20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]-*.txt

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Claude Code session state
.claude/
.claude/audit.log
.claude/hook-debug.log
.claude/backups/

# Large retired / archive folders
archive/
claude_workspace/
mlmodel/
ncaanbamlmodel_v6_archive/
picksbyjonnydashboard/

# Env / secrets (for when you eventually migrate webhook URLs + ODDS_API_KEY)
.env
.env.*
!.env.example
*.key
secrets.json

data/.fuse_hidden*
data/*.db
data/*.db-wal
data/*.db-shm
data/clv_daemon.lock
data/__mount_write_test.tmp
data/backup-*/
pytest-cache-files-*/
diagnostics/
*.log
slice_*.csv
slice_*.txt
engine/_bash_write_test.py
engine/_write_test.py

marketing/
.pytest_tmp/

# Non-engine / personal directories (untracked 2026-06-13 housecleaning)
Jono HQ/
KairosEdge/
memory/
docs for pbj/
*.js
