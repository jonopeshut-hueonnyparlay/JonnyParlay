# JonnyParlay Deep System Audit — 2026-04-28

**Auditor:** Claude (Cowork session)
**Started:** 2026-04-28
**Scope:** Full read of every engine file, all configs, state files, tests, docs.
**Mode:** Read-only. No edits to engine code or pick_log data.
**Prior audit reference:** Closed Apr 21 2026 — 78/78 items (`AUDIT_REPORT*.md` at repo root).

---

## Severity Legend

- **CRIT** — Data loss, financial loss, silent corruption, security exposure
- **HIGH** — Functional bug affecting picks/grading/posting; doc/code drift on production gates
- **MED** — Latent risk, edge-case bug, observability gap
- **LOW** — Polish, code smell, minor inconsistency
- **CLEAN** — Cleanup / cruft removal

---

# PART A — FINDINGS BY PHASE

## Phase 1 — Foundation & Drift

### F1.1 — engine/ ↔ root sync drift [HIGH — sync invariant broken in BOTH directions]
**Rule (CLAUDE.md):** "Always sync to root after edits" — engine/ is source of truth, root copies must match.

**Production import resolution (verified empirically):** When `python run_picks.py` runs from `C:\Users\jono4\Documents\JonnyParlay\` (per CLAUDE.md "Daily Routine"), Python sets `sys.path[0]` to the project root. So `from sgp_builder import ...` and `from secrets_config import ...` etc. **load the ROOT copies, not engine/.**

For 23 of 25 shared files, root and engine match. Two files drift, opposite directions:

#### F1.1a — `secrets_config.py` root copy is **TRUNCATED** [HIGH]
Root file ends with `    f` and "No newline at end of file" — `summary()` is broken (missing return), `__main__` block is missing entirely.

```
engine/secrets_config.py (correct, 6814 B):
    lines.append(f"  ODDS_API_KEY:        {_redact(ODDS_API_KEY)}")
    for short, (env_key, url) in _WEBHOOK_REGISTRY.items():
        lines.append(f"  {env_key:28s} {_redact(url)}")
    return "\n".join(lines)

if __name__ == "__main__":
    print("JonnyParlay secrets inventory:\n")
    print(summary())

root/secrets_config.py (BROKEN, 6579 B, mtime NEWER):
    lines.append(f"  ODDS_API_KEY:        {_redact(ODDS_API_KEY)}")
    f                                   ← truncation garbage
[no for-loop, no return, no __main__]
```

**Practical impact:** Import succeeds (no SyntaxError — `f` is a valid identifier syntactically; only NameError at runtime if `summary()` is called). Production keeps running. But debug summary path is broken.

#### F1.1b — `sgp_builder.py` engine copy is STALE [HIGH]
Reverse direction. Root has the new 3-4 leg / +200-+450 / Gaussian odds / `size_sgp()` design (Apr 28 redesign). Engine still has OLD 6-leg / +400-+700 / fixed 0.25u design.

| Constant | engine/ (OLD, stale) | root (NEW, live) |
|----------|---------------------|--------------------------------|
| MIN_LEGS / MAX_LEGS | 6 / 6 | 3 / 4 |
| MIN_PARLAY_ODDS | 400 | 200 |
| MAX_PARLAY_ODDS | 700 | 450 |
| MIN_LEG_EDGE | 0.005 | 0.010 |
| MIN_LEG_WIN_PROB | 0.55 | 0.65 |
| IDEAL_LEG_WIN_PROB | 0.65 | 0.70 |
| MAX_LEG_ODDS | -150 | -115 |
| Sizing | fixed `SGP_SIZE = 0.25` | `SGP_SIZE_DEFAULT = 0.25`, `SGP_SIZE_PREMIUM = 0.50`, `size_sgp()` |
| Scoring | linear distance from +500 | Gaussian odds curve, composite pool_score |

Production runs root → Apr 28 redesign IS executing ✓. But anyone editing engine/sgp_builder.py and copying engine→root would silently regress the redesign.

#### F1.1c — Why drift happened
Two opposite-direction drifts in 3 days = sync workflow not enforced by tooling. Recommend: replace root copies with one-line shims (`from engine.module import *`) to permanently end drift.

### F1.2 — ~20 engine modules are completely undocumented in CLAUDE.md [MED]
CLAUDE.md describes ~13 files. Reality: 34 Python files in engine/, 23 with root copies. Undocumented modules in active use (~265 KB combined source):
- `backtest_projections.py` (29 KB), `backtest_slice.py` (15 KB)
- `book_names.py`, `brand.py`, `csv_writer.py` (23 KB)
- `discord_guard.py` (8 KB) — Discord post idempotency
- `engine_logger.py` (5 KB), `http_utils.py`, `injury_parser.py` (26 KB)
- `io_utils.py`, `log_setup.py`, `month_names.py`, `morning_preview.py`
- `name_utils.py`, `nba_projector.py` (33 KB), `pick_labels.py`
- `pick_log_io.py` (14 KB), `pick_log_schema.py` (21 KB)
- `projections_db.py` (33 KB), `webhook_fallback.py` (6 KB)

### F1.3 — Custom Projection Engine status mismatch [HIGH]
CLAUDE.md says CPE "Builds in separate Cowork session." Reality: full module suite lives in this repo (~159 KB across 6 modules + 14.8 MB SQLite DB). All uncommitted to git.

### F1.4 — pick_log schema is **28 columns**, not 27 [HIGH]
CLAUDE.md schema lists 27 cols. Actual `data/pick_log.schema.json` declares 28 with `legs` as 28th, `schema_version: 3`. `legs` is the parlay-leg JSON serialization column.

### F1.5 — `.env` missing 3 webhooks declared in `.env.example` [HIGH]
Missing: `DISCORD_FALLBACK_WEBHOOK` (audit H-7 alert path), `DISCORD_LONGSHOT_WEBHOOK`, `DISCORD_SGP_WEBHOOK`. Fallback notifier therefore silent in production.

### F1.6 — Junk artifacts in engine/ [CLEAN]
- `engine/_bash_write_test.py` (10 B, content: `overwrite`)
- `engine/_write_test.py` (35 B, content: `print("hello")`)

### F1.7 — Cruft in repo root [CLEAN]
- `slice_results_2026-04-22*.csv` (~6 MB total) — backtest outputs
- `slice_*.log`, `backtest_*.log` — backtest logs
- `data/__mount_write_test.tmp` (0 B)
- `data/.fuse_hidden*` — ~50+ FUSE deletion artifacts
- `data/backup-pre-audit-fix-20260419-235409/` — pre-audit backup

### F1.8 — Test files at repo root, not in tests/ [LOW]
35 `test_*.py` files cluttering project root. No `tests/` folder.

### F1.9 — KairosEdge folder inside JonnyParlay repo [MED → resolved in F9.1]
Verified isolated.

### F1.10 — `go.bat` / `go.ps1` / `preflight.bat` undocumented [MED]
- `go.ps1` (15.8 KB) — daily run orchestrator, sets `$env:PYTHONIOENCODING = "utf-8"`, dependency check, CSV detection, runs run_picks.py per sport
- `preflight.bat` (5.7 KB) — checks Python ≥ 3.10, deps, lockfiles, CLV daemon task
- `setup_clv_task.bat` (271 B) — sibling to .ps1

### F1.11 — `pandas`/`nba_api`/`nbainjuries` truly soft (verified) [PASS]
No production-path imports. Only CPE modules use them.

### F1.12 — `data/picks/` is mostly large odds_dry JSON dumps [LOW]
8.6 MB / 2.9 MB / 1.0 MB / 5.3 MB / 5.1 MB Odds API debug snapshots. Already gitignored. Growing unbounded.

### F1.13 — clv_daemon.log is 197 KB, log rotation in place [PASS in F5.8]

### F1.14 — git working tree is dirty; significant code uncommitted [CRIT]
**Modified but uncommitted:** CLAUDE.md, run_picks.py + engine/run_picks.py, grade_picks.py + engine/, secrets_config.py + engine/, pick_log_schema.py + engine/, requirements.txt, start_clv_daemon.bat, AUDIT_PROMPT.md, AUDIT_REPORT.md.

**Untracked (never committed):** Entire CPE suite — `engine/nba_projector.py` (33 KB), `engine/projections_db.py` (33 KB), `engine/injury_parser.py` (26 KB), `engine/backtest_projections.py` (29 KB), `engine/backtest_slice.py` (15 KB), `engine/csv_writer.py` (23 KB) = ~159 KB.

**Recent commit history (last 7):** KILLSHOT v2 work + audit Sections 20-41. **Conspicuously missing:** Apr 28 parlay sharpness overhaul, CPE entire module suite, discord_posted.json corruption fix, start_clv_daemon ASCII fix.

**Risk:** SSD failure = total loss of ~$thousands of work.

### F1.15 — "mbp/ folder is RETIRED" partially misleading [LOW]
Folder gone ✓. But "MBP" terminology lives in `run_picks.py:5037` print, R12 docstring, `docs/Master_Betting_Prompt_v9_4.md` (32 KB), `docs/SKILL_v9.4.md`.

### F1.16 — KairosEdge code isolation [PASS]
Zero engine references to "kairos" outside KairosEdge/ directory. Directory contains docs/HTML/JS/ZIPs only.

### F1.17 — .gitignore semantic check [PASS]
All sensitive/local files (`data/*.csv/json/log`, `.env`) correctly ignored.

---

## Phase 2 — Core Engine Correctness (run_picks.py: 5,520 lines)

### F2.1 — KILLSHOT v2 spec implementation [PASS]
Verified every CLAUDE.md requirement against `_passes_killshot_v2_gate`, `_killshot_size`, `select_killshots`:
- ✓ `tier == "T1"` strict (excludes T1B/T2/T3)
- ✓ `pick_score >= 90.0`
- ✓ `win_prob >= 0.65`
- ✓ `odds in [-200, +110]`
- ✓ `stat in {PTS, REB, AST, SOG, 3PM}`
- ✓ Sizing: 3u default, 4u iff wp≥0.70 AND edge≥0.06
- ✓ Weekly cap = 2
- ✓ Manual override requires score≥75 + counts toward cap

### F2.2 — KILLSHOT REB in stat allowlist is dead [LOW]
REB UNDER → tier=T1B, but KILLSHOT requires tier="T1" strict. No REB pick can ever satisfy. Cosmetic redundancy.

### F2.3 — KILLSHOT manual-override matching too greedy [MED]
`tok in full_lower` substring matching. "ja" matches "Jamal Murray", "James Harden", etc. Cap of 2 limits damage but wrong picks could promote.

### F2.4 — Discord guard not marked when suppress_ping=True [HIGH]
Pattern repeats across `post_to_discord`, `post_killshots_to_discord`, `post_daily_lay`, `post_longshot`:

```python
if _webhook_post(...):
    if not suppress_ping:
        _discord_mark_posted(key)
```

`--test` flips suppress_ping=True. Webhook still fires. Guard not marked. Re-run double-posts.

### F2.5 — Pick log positional write omits 28th column for primary picks [LOW (works by accident)]
`log_picks` uses positional `csv.writer.writerow([27 values])` while CANONICAL_HEADER has 28. Works because `legs` is the LAST column (CSV tolerates trailing-empty). Fragile if a new column ever inserts BEFORE `legs`.

### F2.6 — Daily Lay log row missing `legs` field [MED — schema inconsistency]
`_log_longshot` populates `legs` JSON ✓. `_log_daily_lay` does NOT — puts string descriptor in `game` field. Grader must parse `game` as legs string.

### F2.7 — `_killshots_this_week` swallows all exceptions, returns 0 [HIGH]
```python
except Exception:
    return 0
```
Corrupt pick_log → cap check returns 0 → engine posts FULL weekly cap on top of existing KILLSHOTs.

### F2.8 — `auto_r12_from_log` window is 4 days, not 5 [LOW]
Off-by-one. `cutoff = today - timedelta(days=4)`, range `[cutoff, today)` = 4 days.

### F2.9 — `is_decimal_leak()` defined but never called [LOW]
Defensive function exists; never invoked.

### F2.10 — `SIGMA.get(stat, fallback)` masks unknown stats [MED]
Silently uses 40%/2.0 default. Add `logger.warning` on fallback.

### F2.11 — TIERS dict missing KILLSHOT/DAILY_LAY/SGP/LONGSHOT/PARLAY [LOW]
Set as labels post-hoc. `get_tier_min_edge("KILLSHOT")` falls back to default 0.05.

### F2.12 — VAKE_MULT missing entries for KILLSHOT/DAILY_LAY/SGP [LOW]
KILLSHOT bypasses VAKE; DAILY_LAY uses Kelly; SGP uses size_sgp. Cosmetic.

### F2.13 — `round_units` uses Python banker's rounding [LOW]
`round(0.625*4)/4 = 0.5` but `round(0.875*4)/4 = 1.0` due to round-half-to-even. Inconsistent at exact halves. Negligible in practice.

### F2.14 — `parse_csv` empty-CSV defaults sport to "NBA" [MED]
A bad MLB CSV silently returns NBA. Should return UNKNOWN or raise.

### F2.15 — Sports coverage gaps [MED]
- SIGMA: NBA/NHL/NFL(REC only)/MLB. No tennis, NCAAF, NCAAB.
- GAME_SIGMA: NBA/NHL/MLB only. NFL falls through.
- SPORT_UNIT_CAP: NBA/NHL/NFL/MLB. NCAAB/NCAAF/Tennis default to 8.0u.
- TEAM_ALIASES: full NBA + 14 NHL + 0 MLB.
CLAUDE.md claims all sports in scope — partial reality.

### F2.16 — Context Sanity docstring/comment contradiction [LOW]
Inline comment 4541 says "neutral and conflicts are cut" but code (and docstring, and CLAUDE.md) say neutral PASSES.

### F2.17 — `print()` migration (audit S-41) is incomplete [HIGH]
132 print() calls in run_picks.py. Many legitimate CLI output, but several warnings/errors should be `logger.warning/error`:
- Line 51: `_pick_log_lock` timeout warning
- Lines 957, 3131, 3193, 3654, 3657, 3814, 3815, 4009, 4071: various warning/error paths

CLAUDE.md says "78/78 closed" but S-41 is partial.

### F2.18 — Non-ASCII print() crashes on cp1252 console [MED]
Heavy emoji + box-drawing in print(). Production runs through go.ps1 which sets `$env:PYTHONIOENCODING = "utf-8"` ✓ and start_clv_daemon.bat sets it ✓. Direct `python run_picks.py` from vanilla cmd.exe → UnicodeEncodeError on first emoji.

**Fix:** First line of `main()`: `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`.

### F2.19 — Deduplication MLB_CORR_GROUPS comment drift [LOW]
Docstring mentions K/OUTS/HA/ER for pitcher group; actual PITCHER_STATS = {K,OUTS,HA} (no ER).

### F2.20 — `LONGSHOT_MAX_PER_GAME` hardcoded inside function [LOW]
Defined inside `build_safest6_parlay`, not at module level. Tests can't easily override.

### F2.21 — Daily Lay parlay leg-independence assumption [MED]
Combined_prob multiplicative. `book_game_best` keys on `game` so legs are unique by game ✓ but no explicit guard.


### F2.22 — `size_picks_base` Full Card sizing skips VAKE multipliers [MED]
Full Card T3 picks at 9% edge get same 1.25u as T1 picks. Premium gets proper VAKE; Full Card doesn't.

### F2.23 — `apply_caps` SPORT_UNIT_CAP missing sports + 12u total cap [LOW]
NHL 5u asymmetry + 12u total cap not in CLAUDE.md.

### F2.24 — `discord_posted.json` corruption recovery → see F3.2 [HIGH]

### F2.25 — KILLSHOT @everyone gate behavior [PASS]

### F2.26 — Bonus drop NEW pick selection logic [PASS]
Excludes premium card picks (via card_slot non-empty), prior bonus drops, KILLSHOTs. Min score 65 + min wp 0.65. 5/day cap.

### F2.27 — Bonus pick re-sizes via VAKE-style `size_bonus_pick` [PASS]
Eliminates Full Card → Bonus size inflation.

---

## Phase 3 — Data Integrity

### F3.1 — `pick_log_schema.py` docstring out of date [LOW]
Says "27-column schema"; actual `CANONICAL_HEADER` has 28. Schema history block stops at v2 but `SCHEMA_VERSION = 3`.

### F3.2 — `discord_posted.json` corruption recovery NOT implemented [HIGH]
CLAUDE.md says: "if JSON parse fails, rebuild from regex scan of raw bytes. Fixed Apr 28 2026."

Reality (`engine/discord_guard.py:99-104`):
```python
def _load_unlocked() -> dict:
    try:
        with open(GUARD_FILE, "r", encoding="utf-8") as f:
            return json.load(f)
    except (FileNotFoundError, json.JSONDecodeError):
        return {}
```

A `json.JSONDecodeError` silently returns `{}` — resetting **every guard key**. Next run posts: Premium card (with @everyone), POTD, KILLSHOT (with @everyone), Daily lay, Card announcement, Recap. Spam barrage to subscribers.

**The regex-rebuild path documented as the fix doesn't exist anywhere in the codebase.**

### F3.3 — TOCTOU race in run_picks.py guard usage [HIGH]
`engine/discord_guard.py` exports `claim_post(key)` — atomic test-and-set primitive. But run_picks.py uses `_discord_already_posted()` then later `_discord_mark_posted()` (separate lock acquisitions). Classic TOCTOU window.

`morning_preview.py` correctly uses `claim_post` (audit M-2 closed). The pattern exists; it just wasn't propagated.

### F3.4 — Empirical confirmation of F2.4 in the wild [HIGH]
`pick_log.csv` has **9 daily_lay rows**. `discord_posted.json` has **zero `daily_lay:*` keys**. Confirms suppress_ping=True branch never marks the guard. Re-runs would re-post the daily lay.

### F3.5 — `pick_log_mlb.csv` rows from Apr 27+28 short by one column [MED]
282 of 1575 rows have 27 fields (missing trailing `legs` comma). All from Apr 27-28. Cause: positional `writer.writerow([27 values])` writes 27 fields. For pick_log.csv (NBA/NHL), capture_clv periodically rewrites via DictWriter normalizing all rows to 28 fields. For pick_log_mlb (ENABLE_SHADOW_CLV=False), this normalization never runs.

Functional impact: none today (DictReader handles short rows). Risk: if a column is ever inserted BEFORE `legs`, short rows misalign.

### F3.6 — `csv_writer.py:594` uses naive `datetime.now()` (no TZ) [MED]
Single occurrence in entire engine of TZ-naive `datetime.now()`. Used for output filename. Near-midnight runs in MST could write wrong-dated file.

### F3.7 — All other TZ usage uses ZoneInfo correctly [PASS]
Every other date computation uses `ZoneInfo("America/New_York")`.

### F3.8 — All CSV opens use explicit utf-8/utf-8-sig encoding [PASS]

### F3.9 — Pick log volume snapshot [info]
- pick_log.csv: 90 rows since Apr 14 (60 primary + 18 bonus + 9 daily_lay + 5 sgp)
- pick_log_manual.csv: 3 rows
- pick_log_mlb.csv: 1,575 rows

### F3.10 — Schema sidecars all v3, all 28-col [PASS]

### F3.11 — Manual log rows pre-date H-4 validation [LOW]
All 3 rows have `book=""`. Would fail `assert_manual_row_valid` per H-4.

### F3.12 — `legs` column population by run_type [PASS for SGP, MISS for daily_lay/longshot]
| run_type | rows | legs populated |
|----------|------|---------------|
| primary  | 58 | 0 ✓ |
| bonus    | 18 | 0 ✓ |
| daily_lay | 9 | 0 (BUG — F2.6) |
| sgp      | 5 | 5 ✓ |
| longshot | 0 | n/a (no rows yet) |

### F3.13 — `io_utils.atomic_write_json` correct [PASS]
tempfile.mkstemp(dir=parent) + flush + fsync + os.replace ✓.

### F3.14 — Float precision normalizers correct [PASS]
size 2 dec, proj 2 dec, edge 4 dec, odds sign-prefixed, win_prob 4 dec, pick_score 1 dec.

### F3.15 — `discord_posted.json` key formats [info]
recap, premium_card, potd, card_announcement, preview, killshot:date:player:stat:dir:line, sgp:date:game.

### F3.16 — `pick_log_io.py` well-engineered [PASS]
Schema fail-fast ✓, per-process schema warning ✓, FileLock w/ timeout ✓, migrate_row applied ✓.

### F3.17 — `engine_logger.py` exists, S-41 partial [HIGH — see F2.17]

---

## Phase 4 — Grading & Money Math

### F4.1 — Recap embed Apr 28 spec [PASS]
Verified word-for-word:
- ✓ `PROP_RUN_TYPES = {"primary", "bonus"}`
- ✓ `PARLAY_RUN_TYPES = {"daily_lay", "sgp", "longshot"}`
- ✓ Day splits exclude KILLSHOT from props
- ✓ Three independent W-L-PL records (Props / KILLSHOT / Parlays)
- ✓ Week + month footer same 3-way split

### F4.2 — `compute_pl` American-odds math [PASS]
Standard formulas. 4-decimal rounding. Logs warning + returns 0 on unparseable input.

### F4.3 — VOID picks dilute ROI denominator [MED]
```python
risked = sum(_safe_float(p.get("size")) for p in picks if p.get("result") != "P")
```
VOIDed picks (player DNP, stake refunded) included in `risked`. Should also exclude.

### F4.4 — `grade_parlay_legs` mishandles push [MED]
Returns P (whole parlay push) instead of dropping leg & regrading. Books typically drop pushed leg and pay reduced odds. `grade_daily_lay` does it correctly — inconsistency.

### F4.5 — `grade_daily_lay` parses `game` field as legs [HIGH — couples to F2.6]
String parsing of `<team> <signed_spread>` segments split on `/`. Brittle. Fix daily_lay logger to populate `legs` JSON, then converge graders.

### F4.6 — TOCTOU race in grader's guard usage [HIGH — same root as F3.3]

### F4.7 — Monthly auto-fire [PASS]
`if today.day == 1` (in ET). Year-rollover handled.

### F4.8 — `_resolve_pick_is_home` H-4 protection [PASS]

### F4.9 — TERMINAL_RESULTS includes VOID [PASS]

### F4.10 — `_atomic_write_rows` correct [PASS]
FileLock + DictWriter + tmp + fsync + os.replace + sidecar refresh.

### F4.11 — Player-name fuzzy matching collision risk [LOW]
"Brown" twins → weak fallback grabs whichever iterated first. Strong match (first+last) protects most cases.

### F4.12 — Streak inconsistency [LOW]
`compute_streak` (by day) counts manual via COUNTED_RUN_TYPES; `compute_pick_streak` (by pick) excludes manual via MODEL_RUN_TYPES. Pick one rule.

### F4.13 — COUNTED_RUN_TYPES includes empty string and None [LOW]
Defensive — counts grandfathered rows. Minor.

### F4.14 — Heavy emoji in grader [LOW — same as F2.18]

---

## Phase 5 — CLV Daemon & Scheduling

### F5.1 — `capture_clv.py` is well-engineered [PASS for most]
- ✓ Single-instance `FileLock(DAEMON_LOCK_PATH, timeout=0)` + `atexit.register(release)`
- ✓ Lock path overridable via `JONNYPARLAY_DAEMON_LOCK` env var
- ✓ Graceful shutdown — SIGTERM/SIGINT/SIGBREAK/SIGHUP; second signal hard-exits
- ✓ Interruptible sleep (Windows-compatible)
- ✓ Ghost-game checkpoint integrity check on startup
- ✓ Atomic checkpoint via shared `atomic_write_json`
- ✓ FileLock + tmp + fsync + os.replace for pick_log writes
- ✓ Schema sidecar refresh on every successful write
- ✓ Quota exhaustion handling (UTC rollover-based reset)
- ✓ HTTP retry/backoff (3 retries, 2/4/8s, 60s cap)
- ✓ Default UA on all Odds API requests
- ✓ ENABLE_SHADOW_CLV gate (False for MLB)
- ✓ Capture window T-30 to T+3 min
- ✓ Poll: 2 min
- ✓ STALE_AFTER_SECS = 30 min for restart recovery
- ✓ `capture_attempts` LRU-capped at 500 entries
- ✓ Uses logger (not print) for warnings + errors

### F5.2 — capture_clv path resolution doesn't use paths.py [MED]
`SCRIPT_DIR / .parent / "data"` works for `python engine/capture_clv.py`. Root-synced mirror would resolve to `~/Documents/data/` — wrong path. Production avoids via start_clv_daemon.bat which invokes engine/ explicitly.

### F5.3 — `setup_clv_task.ps1` hardcodes install path [LOW]
Should use `$PSScriptRoot`.

### F5.4 — Scheduled task config matches CLAUDE.md spec [PASS]
S4U logon, WakeToRun, StartWhenAvailable, IgnoreNew, 12h limit, batteries OK, Highest run level. Auto-fires on registration.

### F5.5 — `paths.py` adoption only ~13% [MED]
Only `clv_report`, `csv_writer`, `grade_picks`, `projections_db` import paths.py. Other modules still hardcode `~/Documents/JonnyParlay/`. CLAUDE.md says "Every engine tool routes through paths.py" — claim is false.

### F5.6 — `_do_write_closing_odds` preserves on-disk fieldnames [LOW]
Uses fieldnames from on-disk reader, not CANONICAL_HEADER. If pick_log fell behind canonical schema, rewrite preserves old shape.

### F5.7 — `start_clv_daemon.bat` ASCII-clean & sets PYTHONIOENCODING [PASS]
Pure ASCII ✓, PYTHONIOENCODING=utf-8 ✓, python -u ✓, preemptive log rotation BEFORE redirect.

### F5.8 — Log rotation in place [PASS]

### F5.9 — Capture window T-30 to T+3 [PASS]

### F5.10 — `best_price` selects across CO_LEGAL_BOOKS [PASS]
Filters books, direction substring + over/under exact, line within 0.25, picks highest American odds.

### F5.11 — `is_quota_exhausted` self-heals at UTC rollover [PASS]

### F5.12 — CLV math: `closing_implied - your_implied` [PASS]

### F5.13 — Shadow CLV intentionally disabled [info — pairs with F3.5]

### F5.14 — Print statements with non-ASCII in shutdown handler [LOW]

### F5.15 — `_quota_low_warned` test cleanup needed [LOW]

---

## Phase 6 — Discord Pipeline

### F6.1 — `http_utils.py` excellent [PASS]
Mozilla UA + JonnyParlay tag; retry_after_secs prefers HTTP header > body > default; clamped [0.5, 30]s; never raises.

### F6.2 — `morning_preview.py` uses `claim_post` correctly [PASS — counter-example]
Audit M-2 fix wired the atomic test-and-set primitive correctly. **The right pattern exists; just not propagated to run_picks/grade_picks.**

### F6.3 — `morning_preview.py` ASCII `[SKIP]` [PASS]
Replaced multi-codepoint emoji per audit L-2. Other modules still use the emoji form.

### F6.4 — `webhook_fallback.py` exists; alerting DEAD [HIGH]
Notifier correctly designed. `.env` is missing `DISCORD_FALLBACK_WEBHOOK` → notifier silently no-ops. Audit H-7's intent unrealized in production.

### F6.5 — `results_graphic.py` font handling robust [PASS]
JONNYPARLAY_FONTS env var > repo-local fonts/ > OS fallback. `get_font_report()` exposes resolution. Audit M-22 closed.

### F6.6 — `post_nrfi_bonus.py` correctly shadow-gated [PASS]
Shadow sports (MLB) never post to Discord. PICK_LOG H-1 fix.

### F6.7 — `webhook_fallback.notify_fallback` is never invoked [HIGH]
Zero call sites in entire codebase. Audit H-7 closure is fictitious — module exists but isn't connected. Even with the env var set, no failure path calls the notifier.

### F6.8 — `weekly_recap.py` has its own guard pair [LOW]
Not wired to shared claim_post. Same TOCTOU.

### F6.9 — Embed character limits not validated before send [LOW]
No pre-flight check for Discord limits (4096/6000 chars).

### F6.10 — Same emoji/encoding risk across all posters [MED — see F2.18]

---

## Phase 7 — Operational Failure Modes

### F7.1 — Disaster recovery for ~159 KB of untracked code [CRIT — see F1.14]
Single SSD failure loses entire CPE module suite + Apr 28 overhaul edits. Backup cadence: unknown.

### F7.2 — `discord_posted.json` corruption recovery isn't implemented [CRIT — see F3.2]

### F7.3 — Webhook revoke / rotation [PASS]
Just edit .env and re-invoke. Daemons re-read at startup.

### F7.4 — Odds API quota exhaustion [PASS in daemon, F2.17 in run_picks]

### F7.5 — SaberSim CSV format drift [MED]
`parse_csv` silently maps renamed columns to None → empty slate. Add column-validation at parse start.

### F7.6 — Partial-write corruption windows [PASS for main paths]
pick_log.csv/discord_posted.json/clv_checkpoint.json all atomic. F3.5 27-col rows are the one exception.

### F7.7 — DST boundary [PASS]
ZoneInfo handles transitions. Sports games never run at 2:30 AM ET.

### F7.8 — Player name collisions in fuzzy match [LOW — F4.11]

### F7.9 — KILLSHOT cap state [PASS — F2.7 risk]

### F7.10 — Test coverage map [info]
- 35 test files, 536 test functions
- CLAUDE.md says 756/2 — 220-test gap (parametrized? or stale)
- Sections 20-41 fully covered
- **Missing test coverage:** SGP builder, Apr 28 SGP redesign, Daily Lay Apr 28 redesign, Longshot per-game cap, CPE modules, injury_parser, csv_writer
- **Critical bugs found in this audit but not test-covered:** F2.4, F2.7, F3.4, F3.5, F6.7

### F7.11 — Backup snapshots present [info]
`data/backup-pre-audit-fix-20260419-235409/` + `pick_log.backup-pre-manual-split.csv`. No automated process.

### F7.12 — Stale lock file recovery [PASS]
preflight.bat cleans pick_log.csv.lock, clv_daemon.lock, discord_posted.json.lock.

### F7.13 — Concurrent run_picks.py invocations [MED]
No top-level lock. Pick-level dedup ✓ but Discord posts have TOCTOU + Odds API doubled.

### F7.14 — Game cancellation mid-day [MED]
Daemon eventually stops polling at STALE_AFTER_SECS. Grader skips ungraded rows. Manual sweep eventually needed.

---

## Phase 8 — Tests & CLAUDE.md Drift

### F8.1 — Audit-section tests are comprehensive [PASS]
22 dedicated test files for sections 20-41. Each closed audit item has a regression guard.

### F8.2 — Apr 28 parlay overhaul has zero new test coverage [HIGH]
SGP redesign / Daily Lay overhaul / Longshot per-game cap — no `test_sgp_v2.py`, no `test_daily_lay_v2.py`, no `test_longshot.py`. KILLSHOT v2 has dedicated tests; the latest work doesn't.

### F8.3 — CLAUDE.md drift delta [HIGH]

| Topic | CLAUDE.md says | Reality |
|-------|----------------|---------|
| pick_log schema | 27 columns | **28 columns** (`legs` 28th, schema_version=3) |
| audit close | "78/78 items resolved" | **S-41 print→logging is partial** — 132 print() in run_picks.py, multiple warning paths still print() |
| Apr 28 overhaul | "All synced to root" | **engine/sgp_builder.py is STALE** with OLD design |
| Apr 28 overhaul | shipped | **Not committed to git** |
| CPE | "Builds in separate Cowork session" | **CPE code lives in this repo** — uncommitted |
| Discord channels | 13 channels listed | **#longshot, #sgp** webhook env vars exist but channels not in list |
| discord_posted.json | "rebuild from regex scan ... Fixed Apr 28 2026" | **No regex rebuild exists** — returns {} on JSON error |
| paths.py | "Every engine tool routes through it" | **~13% adoption** |
| webhook_fallback (H-7) | Implied complete | **Module exists but `notify_fallback` has zero call sites** |
| LONGSHOT_MAX_PER_GAME=2 | Implied module constant | **Hardcoded inside function** |
| mbp/ folder | "RETIRED" | Folder gone, but **MBP terminology lives in code/docs** |
| `legs` column | Not documented | **Exists as col 28**; populated only for SGP today |
| start_clv_daemon.bat | "ASCII only / fixed Apr 28 2026" | ✓ Confirmed |
| KILLSHOT v2 | All 7 spec items | ✓ Implementation matches exactly |
| Recap embed (Apr 28) | Props/⚡KILLSHOT/Parlays split | ✓ Implementation matches exactly |
| KairosEdge | "Tracked separately" | ✓ Verified isolated |
| MLB shadow | All claims | ✓ Verified |

### F8.4 — Test files at repo root, not in `tests/` [LOW]

### F8.5 — `git log` doesn't reflect recent work [HIGH — F1.14]

### F8.6 — `.env.example` has more entries than `.env` [HIGH — F1.5]

### F8.7-8.10 — Misc doc/asset bloat [LOW]
- ORB / day-trading PDFs in docs/ unrelated to engine
- 20 MB marketing/ assets in repo
- Audit report .md files at root (move to docs/audits/)

---

## Phase 9 — Strategic Separation

### F9.1 — KairosEdge isolation [PASS]
Zero engine references. Doc/HTML/JS/ZIPs only.

### F9.2 — Custom Projection Engine isolation [PASS]
run_picks.py imports zero CPE modules (verified). Production engine path fully decoupled.

### F9.3 — CLAUDE.md location-claim drift [HIGH — F1.3, F8.3]

### F9.4 — CPE schema match needs verification test [info]
No test verifies `csv_writer → parse_csv` round-trip end-to-end. Recommend `test_cpe_schema_match.py`.

### F9.5 — Go-Live Gate not implementable yet [HIGH]
No CLV comparison harness exists for the SaberSim ≥ custom comparison. `backtest_projections.py` may cover; needs verification.

### F9.6 — `data/projections.db` is 14.8 MB [info]
Healthy SQLite WAL with 5 tables (player_index, player_game_logs, injury_status, team_defense, pace_factors).

---

# PART B — CONSOLIDATED PUNCH LIST

## CRIT — fix this week

| ID | File | Issue | Blast | Fix | Effort |
|---|---|---|---|---|---|
| **C1** | git working tree | ~159 KB CPE code + Apr 28 overhaul **uncommitted** (F1.14, F7.1) | Disk failure → total loss | Commit to feature branch, push to remote (verify with `git remote -v`) | 30 min |
| **C2** | engine/discord_guard.py:99-104 | `_load_unlocked` returns `{}` on JSONDecodeError — all guards reset, full daily run reposts to Discord with @everyone (F3.2, F7.2) | One bad write → spam barrage; @everyone rate-limit ban | Implement regex-rebuild from raw bytes per CLAUDE.md spec. Pattern `[a-z_]+:\d{4}-\d{2}-\d{2}` extracts date-stamped keys | 90 min + test |
| **C3** | run_picks.py:3286-3295 + grade_picks.py:1737-1745 + weekly_recap | TOCTOU race — `_discord_already_posted` then `_discord_mark_posted` instead of atomic `claim_post` (F3.3, F4.6) | Two concurrent runs → double Discord posts, double @everyone | Replace with `claim_post(key)` (already exists, used correctly in morning_preview.py) | 2 hr (sweep all call sites) |

## HIGH — fix this sprint

| ID | File | Issue | Fix | Effort |
|---|---|---|---|---|
| **H1** | engine/sgp_builder.py | Engine copy STALE (OLD 6-leg); root has Apr 28 redesign (F1.1b) | Sync engine ← root; commit redesign | 5 min sync + commit |
| **H2** | secrets_config.py (root) | Root copy truncated mid-function (F1.1a) | Copy engine/secrets_config.py over root | 2 min |
| **H3** | run_picks.py post_to_discord, post_killshots, post_daily_lay, post_longshot | Guard not marked when suppress_ping=True. Empirically confirmed: 9 daily_lay rows logged, 0 daily_lay guards (F2.4, F3.4) | Always mark guard on success, OR use separate test-mode guard key | 30 min |
| **H4** | run_picks.py:_killshots_this_week | `except Exception: return 0` lets engine post full weekly cap on top of existing KILLSHOTs (F2.7) | Log exception + return WEEKLY_CAP on read failure | 15 min |
| **H5** | engine/webhook_fallback.notify_fallback | Module exists, zero call sites — H-7 audit close fictitious. Failed primary posts are silent (F6.4, F6.7) | Wire `notify_fallback("morning_preview", err=...)` into every `_webhook_post` failure path. Set `DISCORD_FALLBACK_WEBHOOK` in .env | 90 min |
| **H6** | .env | Missing 3 webhooks declared in .env.example (F1.5) | Create channels (esp. fallback alert), add URLs to .env | 20 min |
| **H7** | engine/run_picks.py | 132 print(); ~10 warning/error paths should be logger (F2.17) | Migrate the warning paths; leave progress prints | 2 hr |
| **H8** | engine/grade_picks.py:daily_stats | `risked` denominator includes VOIDed picks → ROI dilutes (F4.3) | Change filter to `not in ("P", "VOID")` | 5 min + test |
| **H9** | engine/grade_picks.py:grade_daily_lay | Parses `game` field as legs string — fragile (F4.5, F2.6) | Have `_log_daily_lay` populate `legs` JSON; switch grader to `grade_parlay_legs` | 1 hr + test |
| **H10** | CLAUDE.md | 12+ documented claims drift from reality (F8.3) | Update with corrected facts; bump audit status to reflect partial S-41 | 1 hr |
| **H11** | tests | Apr 28 SGP/Daily Lay/Longshot redesigns ZERO test coverage (F8.2) | Add `test_sgp_builder_v2.py`, `test_daily_lay_builder_v2.py`, `test_longshot_builder.py` | 4-6 hr |

## MED — backlog

| ID | File | Issue | Fix | Effort |
|---|---|---|---|---|
| **M1** | run_picks.py:log_picks | Positional writerow → 282 short rows in pick_log_mlb.csv (F3.5) | Switch to `csv.DictWriter(extrasaction="ignore")` | 20 min + test |
| **M2** | engine/csv_writer.py:594 | `datetime.now()` no TZ (F3.6) | Use `datetime.now(ZoneInfo("America/New_York"))` | 2 min |
| **M3** | run_picks.py KILLSHOT manual override | Substring match too greedy (F2.3) | Match full word OR full name only | 15 min |
| **M4** | run_picks.py size_picks_base vs vake | Full Card skips VAKE multipliers (F2.22) | Decide intentional vs apply mults | 1 hr decision + 30 min code |
| **M5** | grade_picks.py grade_parlay_legs | Push leg → P instead of dropping leg (F4.4) | Mirror `grade_daily_lay`'s pushed-leg-drop pattern | 30 min |
| **M6** | run_picks.py parse_csv | Empty CSV defaults to NBA (F2.14) | Return UNKNOWN or raise | 5 min |
| **M7** | run_picks.py SIGMA/GAME_SIGMA/TEAM_ALIASES | Sport coverage gaps (F2.15) | Document or add coverage | TBD |
| **M8** | run_picks.py main() | No top-level lock (F7.13) | `FileLock("data/run_picks.lock", timeout=0)` at start | 5 min |
| **M9** | paths.py adoption | M-26 only ~13% (F5.5) | Sweep replace hardcoded paths | 2 hr |
| **M10** | run_picks.py SPORT_UNIT_CAP | NCAAB/NCAAF/Tennis missing (F2.15) | Add explicit caps | 10 min |
| **M11** | preflight.bat | Add cross-copy md5 check (F1.1c) | Detect future engine↔root drift early | 15 min |
| **M12** | run_picks.py:main() encoding | Direct invoke from cmd.exe crashes on emoji (F2.18) | First line of main: `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` | 3 min |
| **M13** | run_picks.py:_pick_log_lock | Print warning instead of logger.warning (F2.17 sub) | Convert to logger.warning | 5 min |
| **M14** | run_picks.py:_log_daily_lay | Missing `legs` field (F2.6) | Add `_legs_json` for daily_lay | 15 min |

## LOW — polish

| ID | What |
|---|---|
| **L1** | Move 35 `test_*.py` from root → `tests/` directory |
| **L2** | Move `AUDIT_REPORT*.md` + `PICK_LOG_AUDIT.md` → `docs/audits/` |
| **L3** | Move `marketing/` (20 MB) out of repo to cloud storage |
| **L4** | Delete `engine/_bash_write_test.py` and `engine/_write_test.py` |
| **L5** | Clean ~50 `data/.fuse_hidden*` files |
| **L6** | Clean root-level `slice_results_*.csv` + `*.log` backtest artifacts |
| **L7** | Update `pick_log_schema.py` docstring (says 27, is 28; schema_version 3) |
| **L8** | Hoist `LONGSHOT_MAX_PER_GAME=2` to module-level constant |
| **L9** | Drop REB from `KILLSHOT_STAT_ALLOW` (T1 strict means REB never qualifies anyway) |
| **L10** | Remove or invoke `is_decimal_leak()` |
| **L11** | Add `logger.warning` when `SIGMA.get(stat)` falls through |
| **L12** | Document G12 + 12u total daily cap in CLAUDE.md |
| **L13** | `setup_clv_task.ps1`: replace hardcoded path with `$PSScriptRoot` |
| **L14** | Document `legs` column + which run_types populate it in CLAUDE.md |
| **L15** | Document MBP terminology lifecycle in CLAUDE.md |
| **L16** | Replace root .py copies with `from engine.X import *` shims to permanently end drift |
| **L17** | Document daily 12u total cap, NHL 5u asymmetry, NHL SOG=6 in CLAUDE.md |
| **L18** | Fix `auto_r12_from_log` 4-vs-5 day off-by-one (or doc) |
| **L19** | Pick one rule for streak inclusion of manual picks |
| **L20** | Add `test_discord_corruption_recovery.py` for C2 fix |

## CLEAN — repo hygiene

- Add pre-commit hook: `md5sum engine/X.py X.py` for each shared file, fail on mismatch
- Add `git remote -v` check to preflight; warn loudly if no remote configured
- Add `git status --porcelain` check to preflight; warn if uncommitted .py files exist before run
- Daily backup script: copy `data/pick_log*.csv` + `.env` + `data/discord_posted.json` to OneDrive/Dropbox

---

# PART C — AUDIT SUMMARY

**Counts:**
- **CRIT:** 3 items (untracked code, discord guard corruption, TOCTOU race)
- **HIGH:** 11 items
- **MED:** 14 items
- **LOW:** 20 items
- **CLEAN:** 4 items
- **Total findings:** ~52

**Headlines:**

1. **Production is mostly healthy.** Math, gates, sizing, KILLSHOT v2 spec, recap embed structure, CLV daemon, atomic writes — all correct. Apr 28 parlay redesign IS running in production (because production loads root copies, where the redesign lives).

2. **Discord guard layer has multiple latent bugs** that haven't fired in anger because nobody runs concurrent invocations and reruns are rare. C2 + C3 + H3 form a compound risk: a corrupted discord_posted.json + concurrent run + suppress_ping path = guaranteed spam barrage with @everyone.

3. **CLAUDE.md needs a refresh pass.** ~12 documented claims drift from reality. Most importantly: schema is 28 cols not 27, audit S-41 is partial (132 print() calls remain), paths.py adoption is ~13%, discord_posted.json corruption-recovery is fictitious, sgp_builder engine copy is stale.

4. **Disaster recovery is the biggest single risk.** ~159 KB of Custom Projection Engine code lives nowhere except this Windows machine. Apr 28 parlay overhaul + claimed discord_posted.json corruption fix + start_clv_daemon ASCII fix all uncommitted. Single SSD failure = total work loss.

5. **Test coverage is excellent for closed audit items but empty for new work.** Apr 28 SGP/daily-lay/longshot redesigns + Custom Projection Engine all have zero test guards. Most bugs found in this audit (F2.4, F2.7, F3.4, F3.5, F6.7) would have been caught by ~half a day of additional tests.

6. **CLV daemon (capture_clv.py) is the cleanest module in the repo** — graceful shutdown, ghost-game integrity, quota self-heal, atomic writes, proper logging, sidecar refresh. Use as the gold standard pattern for everything else.

**Recommended order of operations:**

1. **Day 1 (60 min):** C1 (commit + push everything), then H2 (fix root secrets_config), then H1 (sync sgp_builder engine ← root)
2. **Day 2 (3-4 hr):** C2 (discord_guard regex rebuild) + C3 (claim_post sweep) + tests for both
3. **Day 3-4:** H3-H11 in priority order; H10 CLAUDE.md refresh in parallel
4. **Sprint backlog:** M1-M14 + L items as time permits

**Audit complete.**
